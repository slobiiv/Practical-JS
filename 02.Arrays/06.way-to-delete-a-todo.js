var todos = ['item 1','item 2','item 3'];

todos.push('item 4'); // pushing(adding) value on the end of the list
todos.push('item 5'); 

// splice() for deleting items in an Array
// first set parameter from where you want to delete item, and second parameter is how many items we want to delete
todos.splice(0,1);